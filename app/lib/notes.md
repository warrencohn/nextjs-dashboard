

```markdown
**Next.js Training**  
Learn more about getting started with Next.js by visiting the [official Next.js learning portal](https://nextjs.org/learn/dashboard-app/getting-started).

**Markdown Cheatsheet**  
For a quick reference to Markdown syntax, check out the [Markdown Guide Cheatsheet](https://www.markdownguide.org/cheat-sheet/).

### Running the Development Server

1. Install the project's packages using:
   ```bash
   pnpm i
   ```
2. Start the development server with:
   ```bash
   pnpm dev
   ```

**Tailwind CSS**  
Explore more about Tailwind CSS at [tailwindcss.com](https://tailwindcss.com/).

### Using the clsx Library to Toggle Class Names

For cases where you need to conditionally style an element based on state or other conditions, the [clsx library](https://www.npmjs.com/package/clsx) simplifies toggling class names. It's recommended to check the documentation for detailed usage, but here's a basic example:

### Other Styling Solutions

In addition to the above methods, you can style your Next.js application using:

- **Sass:** Allows importing of CSS and `.scss` files.
- **CSS-in-JS Libraries:** Such as [styled-jsx](https://github.com/vercel/styled-jsx), [styled-components](https://github.com/vercel/next.js/tree/canary/examples/with-styled-components), and [emotion](https://github.com/vercel/next.js/tree/canary/examples/with-emotion).

For more information, review the CSS documentation in the Next.js guides.

### Why Optimize Fonts?

```

This Markdown text should work as intended in any Markdown processor or editor that supports standard Markdown syntax.